# Auditoría #01 — 2026-04-19 (primera pre-lanzamiento)

## Metadata

| Campo | Valor |
|-------|-------|
| Fecha | 2026-04-19 |
| Auditor | Claude Code (automatizada) + verificación manual de hallazgos 🔴 |
| Rama auditada | `main` @ commit previo a este archivo |
| Motivo | Pre-lanzamiento inicial — primera auditoría del proyecto |
| Alcance | Full OWASP Top 10 (2021) |
| Estado al cierre | 🔴 **NO APROBADA** — 4 hallazgos críticos. 3 cerrados el 2026-04-19 (H-01.2, H-02.1, H-01.1). Falta 1 bloqueante (H-07.1) antes de Auditoría #02 |

---

## Resumen ejecutivo

**Estado al 2026-04-19 post-fix:** 3 FAIL / 7 PASS (iniciaba en 5 FAIL / 5 PASS)

| Categoría | Estado | Severidad | Hallazgo principal |
|-----------|--------|-----------|--------------------|
| A01 Broken Access Control | PASS | 🟢 | H-01.1 (token HMAC-signed + expiración) y H-01.2 (`NEXT_PUBLIC_CRON_SECRET` removido) **cerrados 2026-04-19** |
| A02 Cryptographic Failures | PASS | 🟢 | H-02.1 **cerrado 2026-04-19** — `ADMIN_PASSWORD_HASH` con bcrypt factor 12 |
| A03 Injection | PASS | 🟢 | Query builder de Supabase en todas las rutas, input validado en forms |
| A04 Insecure Design | FAIL | 🟡 MODERATE | Sin rate limiting en `/api/suscripcion-free`, `/api/contactos`, `/api/admin/auth` |
| A05 Security Misconfiguration | FAIL | 🟡 MODERATE | Faltan headers CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy |
| A06 Vulnerable Components | PASS | 🟢 | `npm audit` limpio (0 vulnerabilidades), Dependabot activo |
| A07 Auth Failures | FAIL | 🔴 CRITICAL | Sin rate limit ni account lockout en admin login; vulnerable a brute force |
| A08 Data Integrity | PASS | 🟢 | Migraciones seguras, scrapers con afiliado=true/link correctos tras fix 2026-04-18 |
| A09 Logging & Monitoring | FAIL | 🟡 MODERATE | Sin Sentry/Logtail, sin audit trail de cambios admin |
| A10 SSRF | PASS | 🟢 | Todos los `fetch()` con URLs hardcoded, ningún input de usuario va a URL |

---

## Hallazgos detallados

### A01 — Broken Access Control · 🔴 CRITICAL

**H-01.1 🟢 Token de sesión admin predecible y sin validación server-side — CERRADO 2026-04-19**

Archivo: [lib/admin-auth.ts:16](lib/admin-auth.ts) (antes del fix):
```ts
const token = Buffer.from(`${Date.now()}-${Math.random().toString(36)}`).toString('base64')
```

Problemas:
- `Date.now()` + `Math.random()` generan tokens adivinables (no crypto-safe)
- Base64 no es cifrado — reversible en 1 segundo
- Peor aún: `isAdminAuthenticated()` solo validaba `!!session?.value` — cualquier cookie no vacía pasaba

**Exploit pre-fix:** cualquier atacante con `Cookie: preenvios_admin_session=anything` obtenía acceso total a `/api/admin/*`.

**Fix aplicado (2026-04-19):**

Implementación HMAC-signed stateless token (sin dependencias nuevas, solo `node:crypto`):

1. ✅ **Formato del token:** `<random32-base64url>.<expires-at-unix-ms>.<hmac-sha256-base64url>`
   - `random`: 32 bytes de `crypto.randomBytes` → entropía crypto-safe
   - `expires-at`: timestamp absoluto de expiración (validado server-side)
   - `hmac`: HMAC-SHA256 de `<random>.<expires-at>` con `ADMIN_SESSION_SECRET`

2. ✅ **`createAdminSession()`:** genera los 3 campos, firma con HMAC y persiste el token completo en cookie HttpOnly.

3. ✅ **`isAdminAuthenticated()`:** parsea la cookie en 3 partes, recomputa HMAC y compara con `crypto.timingSafeEqual` (previene timing attacks), valida expiración. Si cualquier check falla → false.

4. ✅ **Duración de sesión reducida de 24h → 4h** (cumple checklist A07 "timeout admin ≤ 4 horas").

5. ✅ **Nueva env var `ADMIN_SESSION_SECRET`** (32 bytes hex). Rotarla invalida todas las sesiones vivas → mecanismo de revocación simple sin tabla en DB.

6. ✅ **Sin fallback:** si `ADMIN_SESSION_SECRET` falta, `sign()` lanza y `verifySignature()` captura y retorna false → login imposible en vez de downgrade silencioso a tokens débiles.

7. ⚠️ **Acción manual en Vercel antes del próximo deploy:**

   a) Generar el secret:
      ```bash
      openssl rand -hex 32
      # o: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
      ```

   b) Vercel → Project Settings → Environment Variables (los 3 entornos):
      - **Agregar** `ADMIN_SESSION_SECRET` = `<salida del comando anterior>` (64 hex chars).

   c) Redeploy. Verificar que el login admin sigue funcionando y que una cookie manipulada devuelve 401.

**Por qué HMAC-signed stateless vs tabla en DB:**
- No requiere migración ni round-trip a Supabase en cada request → dashboard admin más rápido
- Revocación vía rotación del secret (suficiente para 1 admin user)
- Se reemplaza por Supabase Auth en Fase 4.4.B de todos modos — no vale la pena construir infra de sesiones temporal en DB

---

**H-01.2 🟢 `NEXT_PUBLIC_CRON_SECRET` expuesto en bundle cliente — CERRADO 2026-04-19**

Archivo: [app/[locale]/admin/panel.tsx:61](app/%5Blocale%5D/admin/panel.tsx#L61) (antes del fix):
```tsx
const res = await fetch('/api/admin/dashboard', {
  headers: { authorization: `Bearer ${process.env.NEXT_PUBLIC_CRON_SECRET || ''}` }
})
```

Problema:
- Variables con prefijo `NEXT_PUBLIC_*` se inlinean en el JavaScript del bundle que se envía al navegador
- Cualquier visitante de `/es/admin` puede abrir DevTools → Sources y leer el valor
- Con ese secreto un atacante puede invocar `/api/scrape` y disparar scrapers, forzar 3-strikes failure y envenenar datos de todos los operadores

**Fix aplicado (2026-04-19):**
1. ✅ `app/api/admin/dashboard/route.ts` ahora valida con `isAdminAuthenticated()` (session cookie) en vez de `CRON_SECRET`. Eliminado el import de `NextRequest` y el param `request`.
2. ✅ `app/[locale]/admin/panel.tsx:61` — removido el header `authorization`. El fetch usa la cookie HttpOnly que el navegador adjunta automáticamente.
3. ✅ `.env.example` actualizado: comentario explícito indicando que `CRON_SECRET` NUNCA debe llevar prefijo `NEXT_PUBLIC_` y es exclusivo de `/api/scrape`.
4. ✅ **Acción manual en Vercel completada 2026-04-19:** `NEXT_PUBLIC_CRON_SECRET` eliminada de los 3 entornos y `CRON_SECRET` rotada (nuevo valor, solo server). Login admin verificado en producción tras el cambio.

---

**H-01.3 🟢 RLS correctamente activo en todas las tablas de datos**

Migraciones auditadas: 001, 002, 003, 004, 005. Todas las tablas con datos de usuario tienen `ENABLE ROW LEVEL SECURITY`:
- `contactos` (004): 4 políticas denegando todo al anon role ✓
- `suscriptores_free` (001): RLS + políticas apropiadas ✓
- `precios`, `corredores`, `tasas_bancos_centrales`: RLS activo con read-public + write-service-role

`SUPABASE_SERVICE_ROLE_KEY` solo usado en archivos server-side (API routes + `lib/` sin `'use client'`). ✓

---

### A02 — Cryptographic Failures · 🔴 CRITICAL

**H-02.1 🟢 `ADMIN_PASSWORD` comparado en texto plano — CERRADO 2026-04-19**

Archivo: [lib/admin-auth.ts:11-13](lib/admin-auth.ts) (antes del fix):
```ts
export async function validateAdminLogin(email: string, password: string): Promise<boolean> {
  return email === process.env.ADMIN_EMAIL && password === process.env.ADMIN_PASSWORD
}
```

Problema:
- Si el `.env.local` filtra (backup comprometido, log leak, git error, misconfiguración Vercel), el password queda expuesto
- No hay bcrypt/argon2 hashing
- Supabase Auth en Fase 4.4.B mitiga esto, pero hasta entonces es vulnerable

**Fix aplicado (2026-04-19):**
1. ✅ Instalado `bcryptjs@^3` + `@types/bcryptjs` (dev). `npm audit`: 0 vulnerabilidades.
2. ✅ `validateAdminLogin` usa `bcrypt.compareSync(password, process.env.ADMIN_PASSWORD_HASH)` con short-circuit si faltan env vars (return false en vez de lanzar).
3. ✅ `.env.example` actualizado — variable renombrada de `ADMIN_PASSWORD` → `ADMIN_PASSWORD_HASH` con comentario explicando cómo generar el hash.
4. ✅ **Acción manual en Vercel completada 2026-04-19:**
   - `ADMIN_PASSWORD` (plaintext) eliminada de los 3 entornos.
   - `ADMIN_PASSWORD_HASH` (bcrypt factor 12) agregada en los 3 entornos.
   - Login admin verificado en producción con la password original tras el redeploy.

5. ℹ️ **Sin fallback a plaintext:** si `ADMIN_PASSWORD_HASH` falta, el login devuelve 401 siempre — intencional para evitar downgrade silencioso.

**Nota Supabase Auth (Fase 4.4.B):** al migrar, esta función se deprecia entera. Mientras tanto, bcrypt factor 12 con hash en env var es el estándar aceptable.

---

**H-02.2 🟢 Tokens de confirmación email usan crypto seguro**

Archivo [app/api/suscripcion-free/route.ts:55](app/api/suscripcion-free/route.ts):
```ts
const token_confirmacion = crypto.randomUUID()
```
Correcto ✓

---

**H-02.3 🟢 `.gitignore` excluye secretos**

Verificado: `.env.local`, `.env*.local` correctamente excluidos ✓

---

### A03 — Injection · 🟢 PASS (con caveat A03.4)

**H-03.1 🟢 Query builder en todas las rutas**

Todas las queries Supabase usan builder (`.from().select().eq()`, `.insert()`, `.update()`), nunca concatenación de SQL. Auditadas:
- `/api/suscripcion-free/route.ts:40-45`
- `/api/contactos/route.ts:39-41`
- `/api/admin/precios/route.ts:26-35`
- `/api/precios/route.ts` (normalización incluida)

**H-03.2 🟢 Validación server-side en formularios**

`/api/contactos/route.ts:19-37`: length, regex email, whitelist asunto ✓
`/api/suscripcion-free/route.ts:25-35`: email regex, corridor whitelist ✓

**H-03.3 🟢 `dangerouslySetInnerHTML` sin riesgo**

6 usos encontrados (blog, wiki, país, operador, tasa, page SEO). Todos renderizan Schema.org JSON-LD derivado de datos de la DB o arrays estáticos — nunca input de usuario directo. ✓

**H-03.4 🟡 Webhook Twilio NO valida firma HMAC**

Archivo: [app/api/whatsapp/webhook/route.ts:60-81](app/api/whatsapp/webhook/route.ts):
```ts
const body = await request.formData()
const from = body.get('From')
const text = body.get('Body')
// ... procesa sin verificar X-Twilio-Signature
```

Problema: cualquier atacante puede POST al webhook fingiendo ser Twilio, disparar lógica del bot, abusar del endpoint para enumerar corredores, o inyectar respuestas falsas en la cola.

Mitigación parcial: el bot no ejecuta acciones con efectos secundarios graves (solo consulta tasa + responde). Pero el riesgo sube si en el futuro se agrega logging de mensajes o integración con Supabase.

**Fix requerido** (TROUBLESHOOTING/16 ya documenta el patrón):
```ts
import { validateRequest } from 'twilio'
const signature = request.headers.get('x-twilio-signature') || ''
const url = process.env.TWILIO_WEBHOOK_URL!
const params = Object.fromEntries(body.entries())
if (!validateRequest(process.env.TWILIO_AUTH_TOKEN!, signature, url, params)) {
  return NextResponse.json({ error: 'invalid signature' }, { status: 401 })
}
```

---

### A04 — Insecure Design · 🟡 MODERATE

**H-04.1 🟡 Sin rate limiting en endpoints públicos**

Endpoints afectados:
- `POST /api/suscripcion-free` → permite spam masivo de altas + floods vía Resend
- `POST /api/contactos` → permite flood de mensajes en la tabla `contactos`
- `POST /api/admin/auth` → brute force ilimitado (también A07)

**Fix requerido:**
- Instalar `@upstash/ratelimit` + `@upstash/redis` (plan free cubre ~10K req/día)
- Límites sugeridos:
  - `/api/suscripcion-free`: 3 req / 10 min por IP
  - `/api/contactos`: 5 req / 10 min por IP
  - `/api/admin/auth`: 5 intentos / 15 min por IP + respuesta `429 Too Many Requests`

**H-04.2 🟢 Double opt-in implementado**

`/api/suscripcion-free` crea suscripción con `confirmado: false` y solo activa tras GET con token válido ✓

---

### A05 — Security Misconfiguration · 🟡 MODERATE

**H-05.1 🟡 Faltan headers de seguridad globales**

[next.config.ts](next.config.ts) no configura headers. El sitio hoy sirve sin:
- `Strict-Transport-Security` (HSTS)
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `Content-Security-Policy` (CSP)
- `Referrer-Policy: strict-origin-when-cross-origin`

**Fix requerido** (TROUBLESHOOTING/18 ya lo describe). Agregar a `next.config.ts`:
```ts
async headers() {
  return [{
    source: '/:path*',
    headers: [
      { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
      { key: 'X-Content-Type-Options', value: 'nosniff' },
      { key: 'X-Frame-Options', value: 'DENY' },
      { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
      { key: 'Content-Security-Policy', value: [
        "default-src 'self'",
        "script-src 'self' 'unsafe-inline' https://www.googletagmanager.com",
        "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
        "font-src 'self' https://fonts.gstatic.com",
        "img-src 'self' data: https://flagcdn.com https://cdn.brandfetch.io https://www.google-analytics.com",
        "connect-src 'self' https://*.supabase.co https://www.google-analytics.com",
        "frame-ancestors 'none'",
      ].join('; ') },
    ],
  }]
}
```

**H-05.2 🟡 Mensajes de error podrían filtrar info de schema**

Varios `catch` devuelven `error.message` directo de Supabase, que puede incluir nombres de columnas o constraints. No es alto riesgo pero recomendable sanitizar a `"Error interno"` en producción.

---

### A06 — Vulnerable Components · 🟢 PASS

**H-06.1 🟢 `npm audit` limpio**

```
found 0 vulnerabilities
```
Ejecutado el 2026-04-19. Dependencias estables: Next 16.2.4, React 19.2.5, Supabase 2.103.3, Resend 6.12.0, lucide-react 0.542.

**H-06.2 🟢 Dependabot activo**

`.github/dependabot.yml` presente ✓

---

### A07 — Identification and Authentication Failures · 🔴 CRITICAL

**H-07.1 🔴 Admin login sin rate limit ni lockout**

Ya descrito en H-04.1. Agrava el problema de H-02.1 — incluso si el password se hashea, un atacante con diccionario puede intentar 1000 passwords/segundo sin consecuencias.

**Fix:** mismo patrón de `@upstash/ratelimit`. 5 intentos por IP + email por 15 min. Respuesta 429 + log del intento.

**H-07.2 🟡 Cookie `secure` condicionada a NODE_ENV**

Archivo [lib/admin-auth.ts](lib/admin-auth.ts):
```ts
secure: process.env.NODE_ENV === 'production',
```

Correcto para desarrollo local (HTTP) pero fiar del env var es frágil. Un deploy de preview sin `NODE_ENV=production` enviaría cookie sin `Secure`. Hardcodear `secure: true` tras deploy inicial.

**Nota post-fix H-01.1:** la sesión ahora dura solo 4 horas (antes 24h), reduce la ventana de exposición si una cookie se intercepta sobre HTTP por error de config.

---

### A08 — Software and Data Integrity Failures · 🟢 PASS

**H-08.1 🟢 Scrapers con afiliado+link alineados tras fix 2026-04-18**

`lib/scrapers/moneygram.ts:48-49` y `lib/scrapers/westernunion.ts:48-49` tienen `afiliado: true` + link al dominio público. Coinciden con la migración 005 y la normalización de `/api/precios`. No hay regresión. ✓

**H-08.2 🟢 Migraciones numeradas y no-destructivas**

001→005 secuenciales, sin DROPs ni operaciones irreversibles ✓

---

### A09 — Security Logging & Monitoring · 🟡 MODERATE

**H-09.1 🟡 Sin integración de error tracking**

No hay Sentry/Logtail/similar. Errores solo en `console.error()` que Vercel captura pero sin agrupamiento ni alertas.

**Fix recomendado:** `npm install @sentry/nextjs`, configurar DSN en Vercel env. Plan free 5K eventos/mes.

**H-09.2 🟡 Sin audit trail de cambios admin**

`/api/admin/precios` PATCH actualiza tasas sin loggear quién/cuándo/qué cambió. Si alguien compromete la cuenta admin y envenena precios, no hay rastro.

**Fix recomendado:** tabla `admin_audit_log` con columnas `admin_email`, `action`, `entity`, `before`, `after`, `created_at`. Insertar en cada PATCH/POST de admin.

---

### A10 — Server-Side Request Forgery · 🟢 PASS

**H-10.1 🟢 Sin fetch dinámico desde input de usuario**

Todos los `fetch()` auditados usan URLs hardcoded o construidas desde constantes:
- Scrapers: URLs por corredor hardcoded en `CORREDORES` de cada scraper
- Resend: `BASE_URL = 'https://preenvios.com'` hardcoded
- Supabase: URL de env var

Ningún endpoint acepta URL de query param para hacer fetch. ✓

---

## Plan de remediación priorizado

| # | Hallazgo | Severidad | Tiempo estimado | Bloqueante pre-lanzamiento | Estado |
|---|----------|-----------|-----------------|----------------------------|--------|
| 1 | H-01.1 — reemplazar token generation + validación server-side real | 🔴 | 2-3h | **SÍ** | ✅ Cerrado 2026-04-19 |
| 2 | H-01.2 — eliminar `NEXT_PUBLIC_CRON_SECRET`, migrar dashboard a session cookie | 🔴 | 30min | **SÍ** | ✅ Cerrado 2026-04-19 |
| 3 | H-02.1 — bcrypt del `ADMIN_PASSWORD` | 🔴 | 1h | **SÍ** | ✅ Cerrado 2026-04-19 |
| 4 | H-07.1 — rate limit en `/api/admin/auth` | 🔴 | 1-2h | **SÍ** | Abierto |
| 5 | H-03.4 — validación HMAC Twilio webhook | 🟡 | 45min | Recomendado | Abierto |
| 6 | H-04.1 — rate limit en `/api/suscripcion-free` y `/api/contactos` | 🟡 | 1h | Recomendado | Abierto |
| 7 | H-05.1 — headers seguridad en `next.config.ts` | 🟡 | 1h | Recomendado | Abierto |
| 8 | H-09.1 — Sentry integration | 🟡 | 1h | Post-lanzamiento OK | Abierto |
| 9 | H-09.2 — audit trail admin | 🟡 | 2h | Post-lanzamiento OK | Abierto |

**Progreso:** 3 de 4 🔴 bloqueantes cerrados. Resta H-07.1 (rate limit admin login, 1-2h estimadas).

---

## Bitácora de remediación

| Fecha | Hallazgos cerrados | Commit | Notas |
|-------|--------------------|--------|-------|
| 2026-04-19 | H-01.2, H-02.1 | `16350be` | Dashboard migrado a `isAdminAuthenticated()`; bcryptjs instalado y `ADMIN_PASSWORD_HASH` activo. |
| 2026-04-19 | H-01.2, H-02.1 (Vercel) | — | Env vars actualizadas en los 3 entornos: `ADMIN_PASSWORD` eliminada, `ADMIN_PASSWORD_HASH` agregada, `NEXT_PUBLIC_CRON_SECRET` eliminada, `CRON_SECRET` rotada. Login admin probado en producción. |
| 2026-04-19 | H-01.1 | (este commit) | Token sesión admin: HMAC-SHA256 stateless con `ADMIN_SESSION_SECRET` + expiración de 4h + `timingSafeEqual`. Requiere agregar `ADMIN_SESSION_SECRET` en Vercel antes del próximo deploy. |

---

## Estado al cierre

**Auditoría NO APROBADA para activar DNS de preenvios.com.**

Progreso al 2026-04-19: 3 de 4 🔴 bloqueantes cerrados (H-01.2, H-02.1, H-01.1). Resta H-07.1 (rate limit admin login).

La regla del proyecto es clara: "no se aprueba deploy a producción con hallazgos 🔴 abiertos" (ver AUDITORIA_DE_SEGURIDAD/README.md).

**Próxima auditoría:** #02, programada tras cerrar H-07.1. Alcance: re-verificación de A07 (H-07.1) + A03.4 + A04.1 + A05.1. La re-verificación de H-01.1, H-01.2 y H-02.1 ya está documentada en esta auditoría pero debe re-testearse en el entorno Vercel una vez completada la acción manual de agregar `ADMIN_SESSION_SECRET`.

---

## Anexo — Metodología de esta auditoría

- **Análisis estático:** grep/glob sobre toda la codebase buscando patrones conocidos (`Math.random`, `NEXT_PUBLIC_*SECRET*`, `dangerouslySetInnerHTML`, queries SQL crudas, ausencia de validación)
- **Verificación manual:** lectura directa de archivos críticos (`lib/admin-auth.ts`, `app/api/admin/auth/route.ts`, migraciones SQL, `next.config.ts`)
- **Tooling:** `npm audit --audit-level=moderate`
- **Referencia cruzada:** hallazgos previos ya documentados en [TROUBLESHOOTING/14-18](../TROUBLESHOOTING/) usados como punto de partida

## Relacionados

- [README.md](README.md) — metodología completa y cadencia
- [TROUBLESHOOTING/14](../TROUBLESHOOTING/14_endpoint_cron_expuesto.md) — endpoint cron expuesto
- [TROUBLESHOOTING/15](../TROUBLESHOOTING/15_admin_login_vulnerable.md) — admin login vulnerable
- [TROUBLESHOOTING/16](../TROUBLESHOOTING/16_webhook_twilio_sin_firma.md) — webhook Twilio sin firma
- [TROUBLESHOOTING/17](../TROUBLESHOOTING/17_suscripcion_free_spam.md) — suscripción free spam
- [TROUBLESHOOTING/18](../TROUBLESHOOTING/18_headers_seguridad_globales.md) — headers seguridad
